from __future__ import annotations


def download_manifest_only() -> dict[str, object]:
    return {"ok": True, "mode": "manifest-only", "message": "Download nacional deve rodar fora do Next.js/Vercel."}
